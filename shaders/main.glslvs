#version 130

in vec3 in_pos;
in vec2 in_st;

out vec3 vs_out_color;
out vec2 vs_out_st;

uniform mat4 mvp;
uniform float coef_st;


void main()
{
    gl_Position = mvp * vec4(in_pos, 1);
    vs_out_color = vec3(1);
    vs_out_st = in_st * coef_st;
}
