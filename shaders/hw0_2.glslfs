#version 130

in vec3 vs_out_color;

out vec3 o_color;

void main()
{
    int d = 15;
    int xpos = int(gl_FragCoord.x) / d & 1;
    int ypos = int(gl_FragCoord.y) / d & 1;

    if (xpos == ypos)
        o_color = vec3(0, 0, 0);
    else
        o_color = vec3(1, 1, 1);
}
