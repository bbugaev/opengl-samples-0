#version 130

in vec3 position;
in vec3 normal;

out vec3 vs_out_color;

uniform mat4 model;
uniform mat4 mvp;
uniform float time;


void main()
{
    gl_Position = mvp * vec4(position, 1);

    vs_out_color = vec3((model * vec4(normal, 1)).y);
}
